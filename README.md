# 🚀 자동차 경주 미션

---

## ✅ 기능 구현 및 테스트 목록

- 기능 구현 및 해당 기능에 대한 테스트를 완료했다면 체크박스를 선택하세요.

- **자동차 이름 입력 및 유효성 검사**
    - [x] **기능 구현**
        - [x] 쉼표(,)로 자동차 이름 목록 분리
        - [x] 각 이름의 길이가 5자를 초과하는지 검사
        - [x] 예외 발생: 이름 길이 5자 초과
        - [x] 예외 발생: 그 외 잘못된 입력 (e.g., 빈 이름, 공백만 있는 이름)
    - [x] **테스트 코드 작성**
        - [x] 정상적인 이름 목록에 대한 테스트
        - [x] 이름 길이 5자 초과 시 예외 발생 테스트
        - [x] 빈 이름(`a,,b`) 포함 시 예외 발생 테스트

- **시도할 횟수 입력 및 유효성 검사**
    - [X] **기능 구현**
        - [X] 입력값을 숫자로 변환
        - [X] 예외 발생: 숫자가 아닌 값 입력
    - [x] **테스트 코드 작성**
        - [x] 정상적인 숫자 입력에 대한 테스트
        - [x] 숫자가 아닌 값 입력 시 예외 발생 테스트

- **자동차 전진/정지 로직 구현**
    - [X] **기능 구현**
        - [X] 각 자동차에 대해 0~9 사이 무작위 값 생성
        - [X] 무작위 값이 4 이상일 경우 자동차를 전진시킴
    - [ ] **테스트 코드 작성**
        - [ ] 전진 조건(4 이상) 충족 시 위치가 1 증가하는지 테스트
        - [ ] 정지 조건(4 미만) 충족 시 위치가 변하지 않는지 테스트

- **각 차수별 실행 결과 생성**
    - [X] **기능 구현**
        - [X] 각 자동차의 이름과 현재 위치를 문자열로 표현 (e.g., `pobi : --`)
    - [ ] **테스트 코드 작성**
        - [ ] 자동차 위치에 따라 `-`가 올바르게 출력되는지 테스트

- **최종 우승자 판별 및 결과 생성**
    - [X] **기능 구현**
        - [X] 가장 많이 전진한 자동차(들)를 찾음
        - [X] 우승자 목록을 기반으로 최종 결과 문자열 생성 (e.g., `최종 우승자 : pobi, jun`)
    - [ ] **테스트 코드 작성**
        - [ ] 단독 우승자가 발생하는 경우 테스트
        - [ ] 공동 우승자가 발생하는 경우 테스트

---

## 📝 1주차 피드백 반영 체크리스트

- 1주차 공통 피드백을 확인하고, 이번 미션에 잘 적용했는지 체크하세요.

- [ ] 모든 요구사항(기능, 프로그래밍, 과제 진행)을 정확히 준수했는가?
- [ ] `.gitignore`를 설정하여 불필요한 파일(.class, .idea 등)이 Git 관리 대상에서 제외되었는가?
- [ ] 커밋 메시지를 의미 있게(작업 내용이 명확하게)
  작성했는가? ([AngularJS Convention](https://gist.github.com/stephenparish/9941e89d80e2bc58a150) 참고)
- [ ] 기능 단위로 작은 커밋을 만들었는가?
- [ ] 학습 목표(함수 분리, 테스트 작성) 외에 새로운 도전을 시도했는가? (e.g., 새로운 메서드 사용, 5분간 스스로 에러 원인 추측)

---

## 🎯 학습 목표

- 여러 역할을 수행하는 큰 함수를 단일 역할을 수행하는 작은 함수로 분리한다.
- 테스트 도구를 사용하는 방법을 배우고 프로그램이 제대로 작동하는지 테스트한다.
- 1주 차 공통 피드백을 최대한 반영한다.

---

## ⚙️ 프로그래밍 요구 사항

### 프로그래밍 요구 사항 1

- JDK 21 버전에서 실행 가능해야 한다.
- 프로그램 실행의 시작점은 `Application`의 `main()`이다.
- `build.gradle` 파일은 변경할 수 없으며, 제공된 라이브러리 이외의 외부 라이브러리는 사용하지 않는다.
- 프로그램 종료 시 `System.exit()`를 호출하지 않는다.
- 파일, 패키지 등의 이름을 바꾸거나 이동하지 않는다.
- [Java Style Guide](https://google.github.io/styleguide/javaguide.html)를 준수하며 프로그래밍한다.

### 프로그래밍 요구 사항 2

- **들여쓰기(indent) depth는 2까지만 허용한다.** (메서드 분리로 해결)
- **3항 연산자를 쓰지 않는다.**
- **함수(메서드)는 한 가지 일만 하도록 최대한 작게 만든다.**
- **JUnit 5와 AssertJ로 기능 목록이 정상 작동하는지 테스트 코드로 확인한다.**

---

## 📚 라이브러리

- `camp.nextstep.edu.missionutils`의 `Randoms.pickNumberInRange()`와 `Console.readLine()`만 사용한다.

---

## ⌨️ 입출력 요구 사항

### 입력

- 경주할 자동차 이름 (쉼표(,)로 구분): `pobi,woni,jun`
- 시도할 횟수: `5`

### 출력

- 실행 결과 예시

```
경주할 자동차 이름을 입력하세요.(이름은 쉼표(,) 기준으로 구분)
pobi,woni,jun
시도할 횟수는 몇 회인가요?
5

실행 결과
pobi : -
woni : 
jun : -

pobi : --
woni : -
jun : --

pobi : ---
woni : --
jun : ---

<...>

최종 우승자 : pobi, jun
```

---

## 🚀 과제 진행 및 제출 방법

1. `README.md`에 구현할 기능 목록을 정리한다. (현재 단계)
2. 기능 단위로 개발하고, 기능 구현이 완료되면 위 `✅ 기능 구현 및 테스트 목록`에 체크한다.
3. 기능 단위로 `add`, `commit`, `push` 한다.
4. 모든 기능 구현 후, `📝 1주차 피드백 반영 체크리스트`를 최종 점검한다.
5. GitHub PR을 생성하여 제출한다.

---

## 🚨 제출 전 최종 체크리스트

- [ ] 모든 테스트가 성공하는가? (`./gradlew clean test` 실행)
- [ ] 기능 요구 사항에 명시된 입출력 형식을 정확히 따르는가?